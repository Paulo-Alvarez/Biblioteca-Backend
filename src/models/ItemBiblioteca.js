// src/models/ItemBiblioteca.js
class ItemBiblioteca {
    adicionar(item) { throw new Error("Método adicionar não implementado"); }
    listar() { throw new Error("Método listar não implementado"); }
    atualizar(codigo, dados) { throw new Error("Método atualizar não implementado"); }
    deletar(codigo) { throw new Error("Método deletar não implementado"); }
}

module.exports = ItemBiblioteca;